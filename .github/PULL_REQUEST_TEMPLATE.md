## Summary

What does this change, and why?

## Checklist

- [ ] `python -m unittest discover -s tests -v` passes locally
- [ ] `ruff check .` passes locally (if you have `ruff` installed)
- [ ] New behavior has test coverage under `tests/`
- [ ] Docs updated if this changes config, CLI flags, or behavior
      (`README.md`, `docs/CONFIGURATION.md`, `docs/ARCHITECTURE.md`)

## Related issue

Closes #
